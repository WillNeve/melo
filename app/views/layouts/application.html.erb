<!DOCTYPE html>
<html>
  <head>
    <title>Melo</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <% if current_user %>
      <%= render "shared/melo_nav" %>
      <div style='width: 100%; height: 4rem; margin-bottom: 15px;c'></div>
      <%# ^^ this is here to add margin to the content so it doesnt go under fixed navbar %>
      <div data-controller='navbar'>
        <%= render "shared/melo_menu" %>
      </div>
    <% end %>
    <%= render "shared/flashes" %>
    <%= yield %>
  </body>
</html>
